### Example environment variables for development
# DO NOT commit real secrets. Use this file as a template only.

# User credentials for the simple in-memory auth service (development only)
# Set these in your environment or CI secrets for local runs.
STUDENT_PASSWORD=student123
ASSISTANT_PASSWORD=assistant123

# JWT / token settings (optional overrides)
TOKEN_TTL_SECONDS=3600

# Database (example for local development using Postgres)
SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/blueprints
SPRING_DATASOURCE_USERNAME=postgres
SPRING_DATASOURCE_PASSWORD=postgres

# Example RSA keys can be provided via properties or mounted secrets in prod
# RSA_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
# RSA_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
